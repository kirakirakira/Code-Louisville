# Create a virtualenv called myvenv
python3 -m venv myvenv

# Start your virtual environment by running
source myvenv/bin/activate

# Install Django in your virtual environment
pip install --upgrade pip
pip install django
